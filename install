#!/bin/bash

echo "would you like to install hexpmg (y/n)"

read r

if [ "$r" == "y" ]; then
	if command -v axel >/dev/null; then
		echo installing...
		chmod +x ~/hexpmg/hexpmg && sudo mv ~/hexpmg/hexpmg /bin
	else
	   echo "ERROR: you NEED axel to run hexpmg at the moment. would you like to install axel (y/n)" 
	   read e 

	   if [ "$e" == "y" ]; then
		   sudo apt install axel && echo installing... && chmod +x ~/hexpmg/hexpmg && sudo mv ~/hexpmg/hexpmg /bin
	   
	   else
	      exit 1
	   fi
	
	fi
fi


if [ "$r" == "n" ]; then
	exit 1
fi






